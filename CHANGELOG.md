# Changelog

บันทึกการเปลี่ยนแปลงสำคัญของระบบ Football Draw Live (เรียงจากใหม่ไปเก่า)

## 2026-09-16 — รอบน็อกเอาต์ (รอบ 8 ทีม → รอง → ชิง) + responsive มือถือ

Commits: `4908355`, `a453da5` (deploy ขึ้น production `football.siteams.com` แล้ว)

### เพิ่ม / แก้
- **หน้าบันทึกผลรอบน็อกเอาต์** ที่ `/admin/results`: ตารางกรอกสกอร์รอบ 8 ทีม / รอบรอง / รอบชิง
  (บังคับต้องมีผู้ชนะ — เสมอไม่ได้) พร้อมปุ่ม "สร้างคู่รอบถัดไป" ที่สร้างคู่รอบรอง/รอบชิงจากผู้ชนะ
  ให้อัตโนมัติ ผ่าน `POST /api/knockout/advance` โดยไม่ลบ/ไม่ทับคู่หรือผลเดิม
- **Backend:** แยกตรรกะเลื่อนสายเป็นฟังก์ชัน pure `planKnockoutAdvance()` (`apps/api/src/services/knockout.ts`)
  ที่มี unit test คลุม QF→SF→FINAL แล้วเรียกใช้ใน `advanceKnockout` (พฤติกรรมเดิม ไม่เปลี่ยน)
  เพิ่ม integration test คลุมทั้งขั้นตอนเลื่อนสาย
- **แก้เวลารอบ 8 ทีม (คู่ 25–32) วันที่ 19 ก.ย. 2569** ให้ตรงตารางทางการ:
  25 `10:00–10:40`, 26 `10:40–11:20`, 27 `11:20–12:00`, **พักเที่ยง 12:00–13:00**,
  28 `13:00–13:50`, 29 `13:50–14:40`, 30 `14:40–15:30`, 31 `15:30–16:20`, 32 `16:20–17:10`
  (แก้ทั้งใน template `officialSchedule.ts` และในฐานข้อมูลจริง)
- **แก้คู่รอบ 8 ทีม รุ่นอาวุโส 40+ ตามผลจับฉลาก** (จับฉลาก ไม่ใช่ครอสจากตารางคะแนน):
  - คู่ 29: Safe House **vs เพื่อนเยาวชน (s2)** — เดิมระบบสร้างเป็น vs โรงพยาบาลนาแก
  - คู่ 30: **โรงพยาบาลนาแก (s11)** vs VIP.เพื่อนปลาปาก — เดิมระบบสร้างเป็น เพื่อนเยาวชน vs …
  - คู่ 31: พ่อค้านาแก **vs ผึ้งหลวง (s7)** — แทน PB ธาตุพนม (s5)
  - คู่ 32: ปตท.บายพาส นครพนม vs สมประสงค์ FC. (คงเดิม) · รุ่นประชาชน (25–28) ตรงอยู่แล้ว
- **แก้ viewport meta ที่ขาดหายใน `index.html`**: เดิมมือถือ render ที่ ~980px แล้วซูมออก
  ทำให้ media query (responsive) ไม่ทำงานบนเครื่องจริง — เพิ่ม `<meta name="viewport">`
  + `<head>` (charset, lang=th, title) ให้ breakpoints มือถือ/แท็บเล็ต/เดสก์ท็อปทำงาน

### เครื่องมือ / การนำไปใช้งาน (operations)
- `npm run apply:knockout` — สคริปต์ **idempotent, dry-run เป็นค่าเริ่มต้น** สำหรับใส่คู่รอบ 8 ทีม
  ตามผลจับฉลาก + เวลาที่ถูกต้อง ลงฐานข้อมูลที่มีอยู่ โดย **ไม่ทับคู่ที่มีผลแล้ว**
  ยืนยันเขียนจริงด้วย env `CONFIRM_KNOCKOUT_UPDATE=YES` (ดู `scripts/apply-knockout-draw.ts`)
- ทดสอบผ่าน: `npm test` (17 passed) และ `npm run build` (web + api)
- ตรวจแล้วบน production: `/admin/results`, `/rounds`, `/matches`, `/infographic?date=2026-09-19`
  ที่ความกว้าง 390 / 768 / 1440 — ไม่มี horizontal overflow บนหน้า public

### ⚠️ ข้อควรระวัง (สำคัญ)
- **ห้ามรัน `POST /api/knockout/generate` ซ้ำ** หลังมีคู่รอบน็อกเอาต์แล้ว — endpoint จะลบคู่เดิม
  แล้วสร้างใหม่จาก **ตารางคะแนน** ซึ่งจะไม่ตรงกับผลจับฉลาก (คู่รอบ 8 ทีมมาจากการจับฉลาก)
  ถ้าต้องแก้คู่ ให้ใช้ `apply:knockout` แทน
- **ห้ามรัน `POST /api/schedule/official`** — จะติดตั้งตารางใหม่และล้างผล/คู่เดิม
- **ห้ามรัน `npm run prisma:seed` บน production**
